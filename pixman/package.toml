name = "pixman"
version= "0.46.4"
description = "Low-level pixel manipulation routines"
[source]
url = "https://cairographics.org/releases/pixman-0.46.4.tar.gz"
sha256 = "" # releases has sha512, could it work instead of 256?

[dependencies]
runtime = [""]
build = [""]

[build]
system = "ninja"
